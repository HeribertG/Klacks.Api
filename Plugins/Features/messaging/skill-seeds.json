[
  {
    "name": "send_message",
    "description": "Send a message to a person by name via a configured messaging provider (WhatsApp, Telegram, Signal, SMS). Automatically resolves the recipient's phone number from Klacks contacts. Use list_messaging_providers first to see available providers.",
    "category": "Communication",
    "executionType": "Skill",
    "requiredPermissions": ["CanEditSettings"],
    "parameters": [
      {
        "name": "provider",
        "description": "The messaging provider name (e.g., telegram, whatsapp)",
        "type": "String",
        "required": true
      },
      {
        "name": "recipient",
        "description": "Recipient name (e.g., 'Max Muster') or phone number. Names are automatically resolved to phone numbers from Klacks contacts.",
        "type": "String",
        "required": true
      },
      {
        "name": "content",
        "description": "Message text content to send",
        "type": "String",
        "required": true
      },
      {
        "name": "contentType",
        "description": "Content type of the message",
        "type": "Enum",
        "required": false,
        "defaultValue": "text",
        "enumValues": ["text", "image", "document"]
      }
    ],
    "handlerConfig": null,
    "handlerType": null,
    "isEnabled": true,
    "alwaysOn": false,
    "maxCallsPerSession": null,
    "triggerKeywords": ["nachricht", "message", "senden", "send", "whatsapp", "telegram", "signal", "sms", "schreiben", "schick", "benachrichtigen", "notify", "mitteilen", "inform"],
    "version": 5
  },
  {
    "name": "read_messages",
    "description": "Read recent messages from messaging providers. Shows incoming and outgoing messages with sender, content, and timestamp.",
    "category": "Communication",
    "executionType": "Skill",
    "requiredPermissions": ["CanViewSettings"],
    "parameters": [
      {
        "name": "provider",
        "description": "Filter by provider name (e.g., telegram, whatsapp)",
        "type": "String",
        "required": false
      },
      {
        "name": "direction",
        "description": "Filter by message direction",
        "type": "Enum",
        "required": false,
        "defaultValue": "all",
        "enumValues": ["inbound", "outbound", "all"]
      },
      {
        "name": "count",
        "description": "Number of messages to return (1-50)",
        "type": "Integer",
        "required": false,
        "defaultValue": 10
      },
      {
        "name": "sender",
        "description": "Filter by sender phone number or ID",
        "type": "String",
        "required": false
      }
    ],
    "handlerConfig": null,
    "handlerType": null,
    "isEnabled": true,
    "alwaysOn": false,
    "maxCallsPerSession": null,
    "triggerKeywords": ["nachrichten", "messages", "lesen", "read", "inbox", "eingehend", "posteingang"],
    "version": 5
  },
  {
    "name": "list_messaging_providers",
    "description": "List all configured messaging providers with their name, type, and enabled status.",
    "category": "Communication",
    "executionType": "Skill",
    "requiredPermissions": ["CanViewSettings"],
    "parameters": [],
    "handlerConfig": null,
    "handlerType": null,
    "isEnabled": true,
    "alwaysOn": false,
    "maxCallsPerSession": null,
    "triggerKeywords": ["messaging", "provider", "dienste", "services", "kanäle", "channels"],
    "version": 5
  }
]
